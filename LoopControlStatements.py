# A break is used to terminate the loop entirely
# Continue = skips to the next iteration of the loop
# pass= doesnothing just acts as a placeholder

#while True:
 #   name = input('Enter your name ')
  #  if name != '':
#     break

